void print2d(int[2,2] c) {
int i;
int j;
    for(i = 0; i < 2; i++) {
        print("[");
        for(j = 0; j < 2; j++) {
            if(j != 1) {
                print_int(c[i,j]);
                print(",");
            } else {
                print_int(c[i,j]);
            }
        }
        print("]\n");
    }
}

int main() {
    int[2,2] a;
    a = [[1,2],[3,4]];
    print2d(a);
}