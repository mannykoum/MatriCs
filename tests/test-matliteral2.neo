int main() {
	int[2,2] a;
	int[2] b;
	int i;
	int j;

	a = [[1,2],[3,4]];

	/*
	for(i = 0; i < 2; i = i+1) {
		for(j = 0; j < 2; j = j + 1){
		print_int(a[i,j]);
		}
	} 
	*/

	for(i = 0; i < a.dims.dims[0]; i= i+1) {
		print_int(a.dims[i]);
	}
	return 0;
}